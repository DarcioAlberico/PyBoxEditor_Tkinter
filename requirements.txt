# PyBoxEditor — dependências de produção
# Codificação: UTF-8 sem BOM.
# (O arquivo anterior tinha a última linha em UTF-16, e o pip lia "PyMuPDF>=1.23.0"
#  como "P y M u P D F". Não altere a codificação ao editar.)

# --- Núcleo ---
Pillow>=10.0
numpy>=1.26
opencv-python>=4.8
PyMuPDF>=1.23          # leitura/escrita de PDF, medição e embutimento de fontes

# --- Reconhecimento ---
torch>=2.0             # CNN customizada (core/neural_model.py, neural_trainer.py)
easyocr>=1.7           # último elo da cadeia de fallback

# --- Validação de notação ---
chess>=1.10             # regras do jogo (core/notacao.py, ROADMAP F1.7)
                        # Python puro, sem dependências; 1,3 MB instalado.

# --- PDF rasterizado ---
# Exige o binário externo Poppler no PATH. Previsto para remoção (ROADMAP F2.2):
# o PyMuPDF já renderiza páginas nativamente via page.get_pixmap().
pdf2image>=1.17

# --- Opcional ---
# Só é necessário para o caminho Tesseract (menu "Preencher caracteres (OCR)").
# Requer também o executável tesseract.exe instalado no sistema.
pytesseract>=0.3.10

# Removidos por não serem importados por nenhum módulo de produção:
#   imutils, pydantic, python-Levenshtein, rich, PyYAML, platformdirs
