# PyBoxEditor — dependências de desenvolvimento
# Codificação: UTF-8 sem BOM (ver a nota no requirements.txt).
#
#   pip install -r requirements.txt -r requirements-dev.txt
#
# Separado porque o requirements.txt é declaradamente só de produção, e quem
# instala o aplicativo não precisa rodar a suíte.

-r requirements.txt

pytest>=8.0            # a suíte inteira; `pytest` na raiz, ver pytest.ini

fonttools>=4.50        # só o `gerar_fonte_de_simbolos.py` (F62), que recorta a
                       # fonte de símbolos de 641 KB para 5,2 e é rodado à mão.
                       # Fora de produção de propósito: quem exporta um livro
                       # consome o recorte versionado, não o produz. A suíte
                       # também não precisa dele — a cobertura é medida com o
                       # PyMuPDF, que já está em produção.

epubcheck>=5.0         # o EPUB exportado passa por ele em
                       # tests/test_f111_arquivo.py (F111). Pede Java; sem
                       # ele o teste é pulado, e não falha.
